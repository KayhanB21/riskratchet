{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/KayhanB21/riskratchet/schemas/regressions.schema.json",
  "title": "riskratchet check regressions",
  "description": "Output of `riskratchet check --json`. The CLI exits 1 when `regressions` is non-empty, 2 on usage error, 0 otherwise.",
  "type": "object",
  "required": ["$schema", "version", "regressions"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "version": { "type": "string" },
    "regressions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["path", "qualname", "kind", "current_score", "previous_score", "delta", "reason"],
        "additionalProperties": false,
        "properties": {
          "path": { "type": "string" },
          "qualname": { "type": "string" },
          "kind": {
            "enum": [
              "new_above_threshold",
              "regressed",
              "existing_above_threshold",
              "component_regressed",
              "above_threshold"
            ]
          },
          "current_score": { "type": "number", "minimum": 0, "maximum": 100 },
          "previous_score": {
            "type": ["number", "null"],
            "minimum": 0,
            "maximum": 100
          },
          "delta": { "type": ["number", "null"] },
          "reason": { "type": "string" },
          "group": { "type": ["string", "null"], "description": "Monorepo group of the current function (since 0.3.6); null when ungrouped." },
          "source_url": { "type": "string", "description": "Optional blob URL (P10, since 0.2.8); emitted when --repo-url and --commit-ref are passed and the regression has a current snapshot." }
        }
      }
    },
    "baseline": {
      "type": "object",
      "description": "How much of the baseline this run compared (since 0.3.6). Absent in --fail-above (no-baseline) mode.",
      "required": ["entries", "compared", "removed"],
      "additionalProperties": false,
      "properties": {
        "entries": { "type": "integer", "minimum": 0 },
        "compared": { "type": "integer", "minimum": 0 },
        "removed": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
